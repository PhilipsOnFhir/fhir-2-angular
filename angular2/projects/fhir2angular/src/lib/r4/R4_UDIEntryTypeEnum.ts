import { R4_DomainResource } from './R4_DomainResource';

export enum R4_UDIEntryTypeEnum{
    BARCODE = 'barcode',
    CARD = 'card',
    MANUAL = 'manual',
    RFID = 'rfid',
    SELF_REPORTED = 'self-reported',
    UNKNOWN = 'unknown',
}
