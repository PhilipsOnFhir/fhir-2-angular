import { R4_DomainResource } from './R4_DomainResource';

export enum R4_SearchParamTypeEnum{
    COMPOSITE = 'composite',
    DATE = 'date',
    NUMBER = 'number',
    QUANTITY = 'quantity',
    REFERENCE = 'reference',
    SPECIAL = 'special',
    STRING = 'string',
    TOKEN = 'token',
    URI = 'uri',
}
