import { R4_DomainResource } from './R4_DomainResource';

export enum R4_DeviceUseRequestStatusEnum{
    ABORTED = 'aborted',
    ACCEPTED = 'accepted',
    COMPLETED = 'completed',
    IN_PROGRESS = 'in-progress',
    PLANNED = 'planned',
    PROPOSED = 'proposed',
    RECEIVED = 'received',
    REJECTED = 'rejected',
    REQUESTED = 'requested',
    SUSPENDED = 'suspended',
}
