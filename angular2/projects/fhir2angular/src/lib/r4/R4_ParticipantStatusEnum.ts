import { R4_DomainResource } from './R4_DomainResource';

export enum R4_ParticipantStatusEnum{
    ACCEPTED = 'accepted',
    COMPLETED = 'completed',
    DECLINED = 'declined',
    IN_PROCESS = 'in-process',
    NEEDS_ACTION = 'needs-action',
    TENTATIVE = 'tentative',
}
