import { R4_DomainResource } from './R4_DomainResource';

export enum R4_SupplyStatusEnum{
    CANCELLED = 'cancelled',
    DISPENSED = 'dispensed',
    FAILED = 'failed',
    RECEIVED = 'received',
    REQUESTED = 'requested',
}
