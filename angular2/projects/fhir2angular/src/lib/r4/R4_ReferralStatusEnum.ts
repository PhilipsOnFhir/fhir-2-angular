import { R4_DomainResource } from './R4_DomainResource';

export enum R4_ReferralStatusEnum{
    ACCEPTED = 'accepted',
    ACTIVE = 'active',
    CANCELLED = 'cancelled',
    COMPLETED = 'completed',
    DRAFT = 'draft',
    REJECTED = 'rejected',
    REQUESTED = 'requested',
}
