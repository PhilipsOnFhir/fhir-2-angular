import { R4_DomainResource } from './R4_DomainResource';

export enum R4_TestReportStatusEnum{
    COMPLETED = 'completed',
    ENTERED_IN_ERROR = 'entered-in-error',
    IN_PROGRESS = 'in-progress',
    STOPPED = 'stopped',
    WAITING = 'waiting',
}
