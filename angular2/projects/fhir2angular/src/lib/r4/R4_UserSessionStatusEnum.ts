import { R4_DomainResource } from './R4_DomainResource';

export enum R4_UserSessionStatusEnum{
    ACTIVATING = 'activating',
    ACTIVE = 'active',
    CLOSED = 'closed',
    CLOSING = 'closing',
    SUSPENDED = 'suspended',
}
