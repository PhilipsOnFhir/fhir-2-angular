import { R4_DomainResource } from './R4_DomainResource';

export enum R4_ProcedureRequestStatusEnum{
    ABORTED = 'aborted',
    ACCEPTED = 'accepted',
    COMPLETED = 'completed',
    DRAFT = 'draft',
    IN_PROGRESS = 'in-progress',
    PROPOSED = 'proposed',
    RECEIVED = 'received',
    REJECTED = 'rejected',
    REQUESTED = 'requested',
    SUSPENDED = 'suspended',
}
