import { R4_DomainResource } from './R4_DomainResource';

export enum R4_RequestStatusEnum{
    ACTIVE = 'active',
    COMPLETED = 'completed',
    DRAFT = 'draft',
    ENTERED_IN_ERROR = 'entered-in-error',
    ON_HOLD = 'on-hold',
    REVOKED = 'revoked',
    UNKNOWN = 'unknown',
}
