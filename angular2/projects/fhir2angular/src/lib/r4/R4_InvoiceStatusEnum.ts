import { R4_DomainResource } from './R4_DomainResource';

export enum R4_InvoiceStatusEnum{
    BALANCED = 'balanced',
    CANCELLED = 'cancelled',
    DRAFT = 'draft',
    ENTERED_IN_ERROR = 'entered-in-error',
    ISSUED = 'issued',
}
