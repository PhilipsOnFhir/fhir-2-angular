import { R4_DomainResource } from './R4_DomainResource';

export enum R4_StructureMapModelModeEnum{
    PRODUCED = 'produced',
    QUERIED = 'queried',
    SOURCE = 'source',
    TARGET = 'target',
}
