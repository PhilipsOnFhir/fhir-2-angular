import { R4_DomainResource } from './R4_DomainResource';

export enum R4_DeviceMetricCalibrationTypeEnum{
    GAIN = 'gain',
    OFFSET = 'offset',
    TWO_POINT = 'two-point',
    UNSPECIFIED = 'unspecified',
}
