import { R4_DomainResource } from './R4_DomainResource';

export enum R4_ParticipationStatusEnum{
    ACCEPTED = 'accepted',
    DECLINED = 'declined',
    NEEDS_ACTION = 'needs-action',
    TENTATIVE = 'tentative',
}
