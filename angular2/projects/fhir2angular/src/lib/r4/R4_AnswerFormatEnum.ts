import { R4_DomainResource } from './R4_DomainResource';

export enum R4_AnswerFormatEnum{
    ATTACHMENT = 'attachment',
    BOOLEAN = 'boolean',
    CHOICE = 'choice',
    DATE = 'date',
    DATETIME = 'dateTime',
    DECIMAL = 'decimal',
    INSTANT = 'instant',
    INTEGER = 'integer',
    OPEN_CHOICE = 'open-choice',
    QUANTITY = 'quantity',
    REFERENCE = 'reference',
    STRING = 'string',
    TEXT = 'text',
    TIME = 'time',
    URL = 'url',
}
