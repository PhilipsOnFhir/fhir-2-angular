import { R4_DomainResource } from './R4_DomainResource';

export enum R4_AppointmentStatusEnum{
    ARRIVED = 'arrived',
    BOOKED = 'booked',
    CANCELLED = 'cancelled',
    CHECKED_IN = 'checked-in',
    ENTERED_IN_ERROR = 'entered-in-error',
    FULFILLED = 'fulfilled',
    NOSHOW = 'noshow',
    PENDING = 'pending',
    PROPOSED = 'proposed',
    WAITLIST = 'waitlist',
}
