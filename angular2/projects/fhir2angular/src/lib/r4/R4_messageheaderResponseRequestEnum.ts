import { R4_DomainResource } from './R4_DomainResource';

export enum R4_messageheaderResponseRequestEnum{
    ALWAYS = 'always',
    NEVER = 'never',
    ON_ERROR = 'on-error',
    ON_SUCCESS = 'on-success',
}
