import { R4_DomainResource } from './R4_DomainResource';

export enum R4_GraphCompartmentRuleEnum{
    CUSTOM = 'custom',
    DIFFERENT = 'different',
    IDENTICAL = 'identical',
    MATCHING = 'matching',
}
