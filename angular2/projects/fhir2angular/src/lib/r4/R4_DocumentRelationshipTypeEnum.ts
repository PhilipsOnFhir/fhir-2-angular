import { R4_DomainResource } from './R4_DomainResource';

export enum R4_DocumentRelationshipTypeEnum{
    APPENDS = 'appends',
    REPLACES = 'replaces',
    SIGNS = 'signs',
    TRANSFORMS = 'transforms',
}
