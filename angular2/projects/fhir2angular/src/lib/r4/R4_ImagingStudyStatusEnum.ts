import { R4_DomainResource } from './R4_DomainResource';

export enum R4_ImagingStudyStatusEnum{
    AVAILABLE = 'available',
    CANCELLED = 'cancelled',
    ENTERED_IN_ERROR = 'entered-in-error',
    REGISTERED = 'registered',
    UNKNOWN = 'unknown',
}
