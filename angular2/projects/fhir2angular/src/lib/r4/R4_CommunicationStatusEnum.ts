import { R4_DomainResource } from './R4_DomainResource';

export enum R4_CommunicationStatusEnum{
    COMPLETED = 'completed',
    FAILED = 'failed',
    IN_PROGRESS = 'in-progress',
    REJECTED = 'rejected',
    SUSPENDED = 'suspended',
}
