import { R4_DomainResource } from './R4_DomainResource';

export enum R4_SearchModifierCodeEnum{
    ABOVE = 'above',
    BELOW = 'below',
    CONTAINS = 'contains',
    EXACT = 'exact',
    IDENTIFIER = 'identifier',
    IN = 'in',
    MISSING = 'missing',
    NOT = 'not',
    NOT_IN = 'not-in',
    OFTYPE = 'ofType',
    TEXT = 'text',
    TYPE = 'type',
}
