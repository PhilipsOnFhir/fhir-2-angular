import { R4_DomainResource } from './R4_DomainResource';

export enum R4_DiscriminatorTypeEnum{
    EXISTS = 'exists',
    PATTERN = 'pattern',
    PROFILE = 'profile',
    TYPE = 'type',
    VALUE = 'value',
}
