import { R4_DomainResource } from './R4_DomainResource';

export enum R4_medicationrequestStatusEnum{
    ACTIVE = 'active',
    CANCELLED = 'cancelled',
    COMPLETED = 'completed',
    DRAFT = 'draft',
    ENTERED_IN_ERROR = 'entered-in-error',
    ON_HOLD = 'on-hold',
    STOPPED = 'stopped',
    UNKNOWN = 'unknown',
}
