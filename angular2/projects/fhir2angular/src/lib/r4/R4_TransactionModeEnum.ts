import { R4_DomainResource } from './R4_DomainResource';

export enum R4_TransactionModeEnum{
    BATCH = 'batch',
    BOTH = 'both',
    NOT_SUPPORTED = 'not-supported',
    TRANSACTION = 'transaction',
}
