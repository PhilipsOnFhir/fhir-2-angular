import { R4_DomainResource } from './R4_DomainResource';

export enum R4_ObservationRelationshipTypeEnum{
    DERIVED_FROM = 'derived-from',
    HAS_MEMBER = 'has-member',
    INTERFERED_BY = 'interfered-by',
    QUALIFIED_BY = 'qualified-by',
    REPLACES = 'replaces',
    SEQUEL_TO = 'sequel-to',
}
