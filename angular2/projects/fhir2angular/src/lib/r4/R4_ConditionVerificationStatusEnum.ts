import { R4_DomainResource } from './R4_DomainResource';

export enum R4_ConditionVerificationStatusEnum{
    CONFIRMED = 'confirmed',
    DIFFERENTIAL = 'differential',
    ENTERED_IN_ERROR = 'entered-in-error',
    PROVISIONAL = 'provisional',
    REFUTED = 'refuted',
    UNKNOWN = 'unknown',
}
