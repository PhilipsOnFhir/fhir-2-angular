import { R4_DomainResource } from './R4_DomainResource';

export enum R4_DiagnosticOrderStatusEnum{
    ACCEPTED = 'accepted',
    CANCELLED = 'cancelled',
    COMPLETED = 'completed',
    DRAFT = 'draft',
    FAILED = 'failed',
    IN_PROGRESS = 'in-progress',
    PLANNED = 'planned',
    PROPOSED = 'proposed',
    RECEIVED = 'received',
    REJECTED = 'rejected',
    REQUESTED = 'requested',
    REVIEW = 'review',
    SUSPENDED = 'suspended',
}
