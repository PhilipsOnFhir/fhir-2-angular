import { R4_DomainResource } from './R4_DomainResource';

export enum R4_ConditionalReadStatusEnum{
    FULL_SUPPORT = 'full-support',
    MODIFIED_SINCE = 'modified-since',
    NOT_MATCH = 'not-match',
    NOT_SUPPORTED = 'not-supported',
}
