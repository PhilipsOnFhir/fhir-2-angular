import { R4_DomainResource } from './R4_DomainResource';

export enum R4_ReferenceHandlingPolicyEnum{
    ENFORCED = 'enforced',
    LITERAL = 'literal',
    LOCAL = 'local',
    LOGICAL = 'logical',
    RESOLVES = 'resolves',
}
