import { R4_DomainResource } from './R4_DomainResource';

export enum R4_OrderStatusEnum{
    ABORTED = 'aborted',
    ACCEPTED = 'accepted',
    CANCELLED = 'cancelled',
    COMPLETED = 'completed',
    ERROR = 'error',
    PENDING = 'pending',
    REJECTED = 'rejected',
    REPLACED = 'replaced',
    REVIEW = 'review',
}
