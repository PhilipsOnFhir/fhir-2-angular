import { R4_DomainResource } from './R4_DomainResource';

export enum R4_StructureMapSourceListModeEnum{
    FIRST = 'first',
    LAST = 'last',
    NOT_FIRST = 'not_first',
    NOT_LAST = 'not_last',
    ONLY_ONE = 'only_one',
}
