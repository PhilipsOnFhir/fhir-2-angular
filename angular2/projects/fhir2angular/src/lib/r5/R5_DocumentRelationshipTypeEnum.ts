import { R5_DomainResource } from './R5_DomainResource';

export enum R5_DocumentRelationshipTypeEnum{
    APPENDS = 'appends',
    REPLACES = 'replaces',
    SIGNS = 'signs',
    TRANSFORMS = 'transforms',
}
