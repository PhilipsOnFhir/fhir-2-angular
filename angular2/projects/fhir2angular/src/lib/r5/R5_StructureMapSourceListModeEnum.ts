import { R5_DomainResource } from './R5_DomainResource';

export enum R5_StructureMapSourceListModeEnum{
    FIRST = 'first',
    LAST = 'last',
    NOT_FIRST = 'not_first',
    NOT_LAST = 'not_last',
    ONLY_ONE = 'only_one',
}
