import { R5_DomainResource } from './R5_DomainResource';

export enum R5_DiscriminatorTypeEnum{
    EXISTS = 'exists',
    PATTERN = 'pattern',
    PROFILE = 'profile',
    TYPE = 'type',
    VALUE = 'value',
}
