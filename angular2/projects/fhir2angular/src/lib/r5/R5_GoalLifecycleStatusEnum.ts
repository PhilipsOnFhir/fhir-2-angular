import { R5_DomainResource } from './R5_DomainResource';

export enum R5_GoalLifecycleStatusEnum{
    ACCEPTED = 'accepted',
    ACTIVE = 'active',
    CANCELLED = 'cancelled',
    COMPLETED = 'completed',
    ENTERED_IN_ERROR = 'entered-in-error',
    ON_HOLD = 'on-hold',
    PLANNED = 'planned',
    PROPOSED = 'proposed',
    REJECTED = 'rejected',
}
