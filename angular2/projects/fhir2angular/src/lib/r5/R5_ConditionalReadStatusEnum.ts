import { R5_DomainResource } from './R5_DomainResource';

export enum R5_ConditionalReadStatusEnum{
    FULL_SUPPORT = 'full-support',
    MODIFIED_SINCE = 'modified-since',
    NOT_MATCH = 'not-match',
    NOT_SUPPORTED = 'not-supported',
}
