import { R5_DomainResource } from './R5_DomainResource';

export enum R5_ReferenceHandlingPolicyEnum{
    ENFORCED = 'enforced',
    LITERAL = 'literal',
    LOCAL = 'local',
    LOGICAL = 'logical',
    RESOLVES = 'resolves',
}
