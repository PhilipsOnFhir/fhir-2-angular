import { R5_DomainResource } from './R5_DomainResource';

export enum R5_SearchParamTypeEnum{
    COMPOSITE = 'composite',
    DATE = 'date',
    NUMBER = 'number',
    QUANTITY = 'quantity',
    REFERENCE = 'reference',
    SPECIAL = 'special',
    STRING = 'string',
    TOKEN = 'token',
    URI = 'uri',
}
