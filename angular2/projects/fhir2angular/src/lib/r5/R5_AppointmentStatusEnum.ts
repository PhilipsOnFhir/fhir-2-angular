import { R5_DomainResource } from './R5_DomainResource';

export enum R5_AppointmentStatusEnum{
    ARRIVED = 'arrived',
    BOOKED = 'booked',
    CANCELLED = 'cancelled',
    CHECKED_IN = 'checked-in',
    ENTERED_IN_ERROR = 'entered-in-error',
    FULFILLED = 'fulfilled',
    NOSHOW = 'noshow',
    PENDING = 'pending',
    PROPOSED = 'proposed',
    WAITLIST = 'waitlist',
}
