import { R5_DomainResource } from './R5_DomainResource';

export enum R5_SearchModifierCodeEnum{
    ABOVE = 'above',
    BELOW = 'below',
    CONTAINS = 'contains',
    EXACT = 'exact',
    IDENTIFIER = 'identifier',
    IN = 'in',
    MISSING = 'missing',
    NOT = 'not',
    NOT_IN = 'not-in',
    OFTYPE = 'ofType',
    TEXT = 'text',
    TYPE = 'type',
}
