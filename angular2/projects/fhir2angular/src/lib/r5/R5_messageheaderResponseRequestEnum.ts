import { R5_DomainResource } from './R5_DomainResource';

export enum R5_messageheaderResponseRequestEnum{
    ALWAYS = 'always',
    NEVER = 'never',
    ON_ERROR = 'on-error',
    ON_SUCCESS = 'on-success',
}
