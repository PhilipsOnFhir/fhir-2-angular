import { R5_DomainResource } from './R5_DomainResource';

export enum R5_ParticipationStatusEnum{
    ACCEPTED = 'accepted',
    DECLINED = 'declined',
    NEEDS_ACTION = 'needs-action',
    TENTATIVE = 'tentative',
}
