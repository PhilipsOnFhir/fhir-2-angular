import { R5_DomainResource } from './R5_DomainResource';

export enum R5_DeviceMetricCalibrationTypeEnum{
    GAIN = 'gain',
    OFFSET = 'offset',
    TWO_POINT = 'two-point',
    UNSPECIFIED = 'unspecified',
}
