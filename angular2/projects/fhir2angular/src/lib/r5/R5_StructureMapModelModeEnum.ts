import { R5_DomainResource } from './R5_DomainResource';

export enum R5_StructureMapModelModeEnum{
    PRODUCED = 'produced',
    QUERIED = 'queried',
    SOURCE = 'source',
    TARGET = 'target',
}
