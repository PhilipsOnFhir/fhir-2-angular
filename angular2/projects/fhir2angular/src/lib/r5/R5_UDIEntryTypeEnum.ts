import { R5_DomainResource } from './R5_DomainResource';

export enum R5_UDIEntryTypeEnum{
    BARCODE = 'barcode',
    CARD = 'card',
    MANUAL = 'manual',
    RFID = 'rfid',
    SELF_REPORTED = 'self-reported',
    UNKNOWN = 'unknown',
}
