import { R5_DomainResource } from './R5_DomainResource';

export enum R5_UserSessionStatusEnum{
    ACTIVATING = 'activating',
    ACTIVE = 'active',
    CLOSED = 'closed',
    CLOSING = 'closing',
    SUSPENDED = 'suspended',
}
