import { R5_DomainResource } from './R5_DomainResource';

export enum R5_EventStatusEnum{
    COMPLETED = 'completed',
    ENTERED_IN_ERROR = 'entered-in-error',
    IN_PROGRESS = 'in-progress',
    NOT_DONE = 'not-done',
    ON_HOLD = 'on-hold',
    PREPARATION = 'preparation',
    STOPPED = 'stopped',
    UNKNOWN = 'unknown',
}
