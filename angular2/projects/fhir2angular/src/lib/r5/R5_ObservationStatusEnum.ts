import { R5_DomainResource } from './R5_DomainResource';

export enum R5_ObservationStatusEnum{
    AMENDED = 'amended',
    CANCELLED = 'cancelled',
    CORRECTED = 'corrected',
    ENTERED_IN_ERROR = 'entered-in-error',
    FINAL = 'final',
    PRELIMINARY = 'preliminary',
    REGISTERED = 'registered',
    UNKNOWN = 'unknown',
}
