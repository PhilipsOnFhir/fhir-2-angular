import { R5_DomainResource } from './R5_DomainResource';

export enum R5_medicationrequestStatusEnum{
    ACTIVE = 'active',
    CANCELLED = 'cancelled',
    COMPLETED = 'completed',
    DRAFT = 'draft',
    ENTERED_IN_ERROR = 'entered-in-error',
    ON_HOLD = 'on-hold',
    STOPPED = 'stopped',
    UNKNOWN = 'unknown',
}
