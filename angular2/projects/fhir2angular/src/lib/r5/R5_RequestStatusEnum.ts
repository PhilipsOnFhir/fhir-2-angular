import { R5_DomainResource } from './R5_DomainResource';

export enum R5_RequestStatusEnum{
    ACTIVE = 'active',
    COMPLETED = 'completed',
    DRAFT = 'draft',
    ENTERED_IN_ERROR = 'entered-in-error',
    ON_HOLD = 'on-hold',
    REVOKED = 'revoked',
    UNKNOWN = 'unknown',
}
