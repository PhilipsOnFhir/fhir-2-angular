import { DSTU2_DomainResource } from './DSTU2_DomainResource';

export enum DSTU2_SearchParamTypeEnum{
    COMPOSITE = 'composite',
    DATE = 'date',
    NUMBER = 'number',
    QUANTITY = 'quantity',
    REFERENCE = 'reference',
    STRING = 'string',
    TOKEN = 'token',
    URI = 'uri',
}
