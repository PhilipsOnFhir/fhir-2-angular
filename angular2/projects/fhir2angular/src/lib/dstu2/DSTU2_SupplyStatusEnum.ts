import { DSTU2_DomainResource } from './DSTU2_DomainResource';

export enum DSTU2_SupplyStatusEnum{
    CANCELLED = 'cancelled',
    DISPENSED = 'dispensed',
    FAILED = 'failed',
    RECEIVED = 'received',
    REQUESTED = 'requested',
}
