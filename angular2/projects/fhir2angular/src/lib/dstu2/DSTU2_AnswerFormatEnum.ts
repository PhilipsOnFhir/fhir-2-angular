import { DSTU2_DomainResource } from './DSTU2_DomainResource';

export enum DSTU2_AnswerFormatEnum{
    ATTACHMENT = 'attachment',
    BOOLEAN = 'boolean',
    CHOICE = 'choice',
    DATE = 'date',
    DATETIME = 'dateTime',
    DECIMAL = 'decimal',
    INSTANT = 'instant',
    INTEGER = 'integer',
    OPEN_CHOICE = 'open-choice',
    QUANTITY = 'quantity',
    REFERENCE = 'reference',
    STRING = 'string',
    TEXT = 'text',
    TIME = 'time',
    URL = 'url',
}
