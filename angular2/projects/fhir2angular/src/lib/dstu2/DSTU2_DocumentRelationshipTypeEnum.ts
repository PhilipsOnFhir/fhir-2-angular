import { DSTU2_DomainResource } from './DSTU2_DomainResource';

export enum DSTU2_DocumentRelationshipTypeEnum{
    APPENDS = 'appends',
    REPLACES = 'replaces',
    SIGNS = 'signs',
    TRANSFORMS = 'transforms',
}
