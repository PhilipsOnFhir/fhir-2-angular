import { DSTU2_DomainResource } from './DSTU2_DomainResource';

export enum DSTU2_CarePlanActivityStatusEnum{
    CANCELLED = 'cancelled',
    COMPLETED = 'completed',
    IN_PROGRESS = 'in-progress',
    NOT_STARTED = 'not-started',
    ON_HOLD = 'on-hold',
    SCHEDULED = 'scheduled',
}
