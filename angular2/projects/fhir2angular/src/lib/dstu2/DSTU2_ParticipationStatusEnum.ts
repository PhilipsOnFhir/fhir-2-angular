import { DSTU2_DomainResource } from './DSTU2_DomainResource';

export enum DSTU2_ParticipationStatusEnum{
    ACCEPTED = 'accepted',
    DECLINED = 'declined',
    NEEDS_ACTION = 'needs-action',
    TENTATIVE = 'tentative',
}
