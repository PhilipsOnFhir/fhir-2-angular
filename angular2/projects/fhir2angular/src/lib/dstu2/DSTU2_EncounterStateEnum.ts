import { DSTU2_DomainResource } from './DSTU2_DomainResource';

export enum DSTU2_EncounterStateEnum{
    ARRIVED = 'arrived',
    CANCELLED = 'cancelled',
    FINISHED = 'finished',
    IN_PROGRESS = 'in-progress',
    ONLEAVE = 'onleave',
    PLANNED = 'planned',
}
