import { DSTU2_DomainResource } from './DSTU2_DomainResource';

export enum DSTU2_SearchModifierCodeEnum{
    ABOVE = 'above',
    BELOW = 'below',
    CONTAINS = 'contains',
    EXACT = 'exact',
    IN = 'in',
    MISSING = 'missing',
    NOT = 'not',
    NOT_IN = 'not-in',
    TEXT = 'text',
    TYPE = 'type',
}
