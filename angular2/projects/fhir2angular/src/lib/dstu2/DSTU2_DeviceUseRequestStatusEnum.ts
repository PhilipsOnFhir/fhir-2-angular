import { DSTU2_DomainResource } from './DSTU2_DomainResource';

export enum DSTU2_DeviceUseRequestStatusEnum{
    ABORTED = 'aborted',
    ACCEPTED = 'accepted',
    COMPLETED = 'completed',
    IN_PROGRESS = 'in-progress',
    PLANNED = 'planned',
    PROPOSED = 'proposed',
    RECEIVED = 'received',
    REJECTED = 'rejected',
    REQUESTED = 'requested',
    SUSPENDED = 'suspended',
}
