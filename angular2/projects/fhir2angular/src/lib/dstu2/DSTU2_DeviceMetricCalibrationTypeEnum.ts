import { DSTU2_DomainResource } from './DSTU2_DomainResource';

export enum DSTU2_DeviceMetricCalibrationTypeEnum{
    GAIN = 'gain',
    OFFSET = 'offset',
    TWO_POINT = 'two-point',
    UNSPECIFIED = 'unspecified',
}
