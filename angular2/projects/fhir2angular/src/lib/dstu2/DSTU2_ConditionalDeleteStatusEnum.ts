import { DSTU2_DomainResource } from './DSTU2_DomainResource';

export enum DSTU2_ConditionalDeleteStatusEnum{
    MULTIPLE = 'multiple',
    NOT_SUPPORTED = 'not-supported',
    SINGLE = 'single',
}
