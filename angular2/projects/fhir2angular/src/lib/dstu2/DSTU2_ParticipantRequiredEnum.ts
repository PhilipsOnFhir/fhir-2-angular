import { DSTU2_DomainResource } from './DSTU2_DomainResource';

export enum DSTU2_ParticipantRequiredEnum{
    INFORMATION_ONLY = 'information-only',
    OPTIONAL = 'optional',
    REQUIRED = 'required',
}
