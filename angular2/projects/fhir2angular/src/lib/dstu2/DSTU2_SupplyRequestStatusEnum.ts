import { DSTU2_DomainResource } from './DSTU2_DomainResource';

export enum DSTU2_SupplyRequestStatusEnum{
    CANCELLED = 'cancelled',
    COMPLETED = 'completed',
    FAILED = 'failed',
    REQUESTED = 'requested',
}
