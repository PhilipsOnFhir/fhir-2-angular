import { DSTU2_DomainResource } from './DSTU2_DomainResource';

export enum DSTU2_DiagnosticOrderStatusEnum{
    ACCEPTED = 'accepted',
    CANCELLED = 'cancelled',
    COMPLETED = 'completed',
    DRAFT = 'draft',
    FAILED = 'failed',
    IN_PROGRESS = 'in-progress',
    PLANNED = 'planned',
    PROPOSED = 'proposed',
    RECEIVED = 'received',
    REJECTED = 'rejected',
    REQUESTED = 'requested',
    REVIEW = 'review',
    SUSPENDED = 'suspended',
}
