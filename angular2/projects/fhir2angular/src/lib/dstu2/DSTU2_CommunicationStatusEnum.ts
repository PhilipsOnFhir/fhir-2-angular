import { DSTU2_DomainResource } from './DSTU2_DomainResource';

export enum DSTU2_CommunicationStatusEnum{
    COMPLETED = 'completed',
    FAILED = 'failed',
    IN_PROGRESS = 'in-progress',
    REJECTED = 'rejected',
    SUSPENDED = 'suspended',
}
