import { DSTU2_DomainResource } from './DSTU2_DomainResource';

export enum DSTU2_MedicationDispenseStatusEnum{
    COMPLETED = 'completed',
    ENTERED_IN_ERROR = 'entered-in-error',
    IN_PROGRESS = 'in-progress',
    ON_HOLD = 'on-hold',
    STOPPED = 'stopped',
}
