import { DSTU2_DomainResource } from './DSTU2_DomainResource';

export enum DSTU2_DiagnosticReportStatusEnum{
    APPENDED = 'appended',
    CANCELLED = 'cancelled',
    CORRECTED = 'corrected',
    ENTERED_IN_ERROR = 'entered-in-error',
    FINAL = 'final',
    PARTIAL = 'partial',
    REGISTERED = 'registered',
}
