import { DSTU2_DomainResource } from './DSTU2_DomainResource';

export enum DSTU2_ReferralStatusEnum{
    ACCEPTED = 'accepted',
    ACTIVE = 'active',
    CANCELLED = 'cancelled',
    COMPLETED = 'completed',
    DRAFT = 'draft',
    REJECTED = 'rejected',
    REQUESTED = 'requested',
}
