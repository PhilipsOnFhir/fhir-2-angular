import { DSTU2_DomainResource } from './DSTU2_DomainResource';

export enum DSTU2_ParticipantStatusEnum{
    ACCEPTED = 'accepted',
    COMPLETED = 'completed',
    DECLINED = 'declined',
    IN_PROCESS = 'in-process',
    NEEDS_ACTION = 'needs-action',
    TENTATIVE = 'tentative',
}
