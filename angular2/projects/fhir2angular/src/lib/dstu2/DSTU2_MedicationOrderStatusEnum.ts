import { DSTU2_DomainResource } from './DSTU2_DomainResource';

export enum DSTU2_MedicationOrderStatusEnum{
    ACTIVE = 'active',
    COMPLETED = 'completed',
    DRAFT = 'draft',
    ENTERED_IN_ERROR = 'entered-in-error',
    ON_HOLD = 'on-hold',
    STOPPED = 'stopped',
}
