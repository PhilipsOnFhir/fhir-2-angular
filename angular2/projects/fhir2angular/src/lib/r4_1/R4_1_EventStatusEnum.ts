import { R4_1_DomainResource } from './R4_1_DomainResource';

export enum R4_1_EventStatusEnum{
    COMPLETED = 'completed',
    ENTERED_IN_ERROR = 'entered-in-error',
    IN_PROGRESS = 'in-progress',
    NOT_DONE = 'not-done',
    ON_HOLD = 'on-hold',
    PREPARATION = 'preparation',
    STOPPED = 'stopped',
    UNKNOWN = 'unknown',
}
