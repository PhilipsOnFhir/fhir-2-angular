import { R4_1_DomainResource } from './R4_1_DomainResource';

export enum R4_1_messageheaderResponseRequestEnum{
    ALWAYS = 'always',
    NEVER = 'never',
    ON_ERROR = 'on-error',
    ON_SUCCESS = 'on-success',
}
