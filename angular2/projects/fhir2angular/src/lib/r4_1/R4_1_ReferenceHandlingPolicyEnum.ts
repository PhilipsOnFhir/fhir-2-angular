import { R4_1_DomainResource } from './R4_1_DomainResource';

export enum R4_1_ReferenceHandlingPolicyEnum{
    ENFORCED = 'enforced',
    LITERAL = 'literal',
    LOCAL = 'local',
    LOGICAL = 'logical',
    RESOLVES = 'resolves',
}
