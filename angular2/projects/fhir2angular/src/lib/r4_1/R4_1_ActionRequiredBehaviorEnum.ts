import { R4_1_DomainResource } from './R4_1_DomainResource';

export enum R4_1_ActionRequiredBehaviorEnum{
    COULD = 'could',
    MUST = 'must',
    MUST_UNLESS_DOCUMENTED = 'must-unless-documented',
}
