import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_GraphCompartmentRuleEnum{
    CUSTOM = 'custom',
    DIFFERENT = 'different',
    IDENTICAL = 'identical',
    MATCHING = 'matching',
}
