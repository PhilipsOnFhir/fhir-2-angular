import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_SlotStatusEnum{
    BUSY = 'busy',
    BUSY_TENTATIVE = 'busy-tentative',
    BUSY_UNAVAILABLE = 'busy-unavailable',
    ENTERED_IN_ERROR = 'entered-in-error',
    FREE = 'free',
}
