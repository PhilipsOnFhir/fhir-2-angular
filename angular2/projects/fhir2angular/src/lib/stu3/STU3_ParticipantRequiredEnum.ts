import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_ParticipantRequiredEnum{
    INFORMATION_ONLY = 'information-only',
    OPTIONAL = 'optional',
    REQUIRED = 'required',
}
