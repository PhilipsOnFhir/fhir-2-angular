import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_CodeSystemContentModeEnum{
    COMPLETE = 'complete',
    EXAMPLE = 'example',
    FRAGMENT = 'fragment',
    NOT_PRESENT = 'not-present',
}
