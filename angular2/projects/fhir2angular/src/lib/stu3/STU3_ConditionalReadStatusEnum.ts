import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_ConditionalReadStatusEnum{
    FULL_SUPPORT = 'full-support',
    MODIFIED_SINCE = 'modified-since',
    NOT_MATCH = 'not-match',
    NOT_SUPPORTED = 'not-supported',
}
