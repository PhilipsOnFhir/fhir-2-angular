import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_DocumentRelationshipTypeEnum{
    APPENDS = 'appends',
    REPLACES = 'replaces',
    SIGNS = 'signs',
    TRANSFORMS = 'transforms',
}
