import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_ResearchStudyStatusEnum{
    COMPLETED = 'completed',
    DRAFT = 'draft',
    ENTERED_IN_ERROR = 'entered-in-error',
    IN_PROGRESS = 'in-progress',
    STOPPED = 'stopped',
    SUSPENDED = 'suspended',
}
