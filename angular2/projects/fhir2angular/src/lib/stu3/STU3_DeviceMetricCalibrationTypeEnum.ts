import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_DeviceMetricCalibrationTypeEnum{
    GAIN = 'gain',
    OFFSET = 'offset',
    TWO_POINT = 'two-point',
    UNSPECIFIED = 'unspecified',
}
