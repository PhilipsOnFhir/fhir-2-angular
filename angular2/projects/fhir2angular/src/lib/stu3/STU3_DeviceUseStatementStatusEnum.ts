import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_DeviceUseStatementStatusEnum{
    ACTIVE = 'active',
    COMPLETED = 'completed',
    ENTERED_IN_ERROR = 'entered-in-error',
    INTENDED = 'intended',
    ON_HOLD = 'on-hold',
    STOPPED = 'stopped',
}
