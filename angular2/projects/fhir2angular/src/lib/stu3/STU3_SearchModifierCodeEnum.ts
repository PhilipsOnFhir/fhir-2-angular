import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_SearchModifierCodeEnum{
    ABOVE = 'above',
    BELOW = 'below',
    CONTAINS = 'contains',
    EXACT = 'exact',
    IN = 'in',
    MISSING = 'missing',
    NOT = 'not',
    NOT_IN = 'not-in',
    TEXT = 'text',
    TYPE = 'type',
}
