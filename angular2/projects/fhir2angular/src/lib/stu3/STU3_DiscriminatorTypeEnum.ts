import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_DiscriminatorTypeEnum{
    EXISTS = 'exists',
    PATTERN = 'pattern',
    PROFILE = 'profile',
    TYPE = 'type',
    VALUE = 'value',
}
