import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_SearchParamTypeEnum{
    COMPOSITE = 'composite',
    DATE = 'date',
    NUMBER = 'number',
    QUANTITY = 'quantity',
    REFERENCE = 'reference',
    STRING = 'string',
    TOKEN = 'token',
    URI = 'uri',
}
