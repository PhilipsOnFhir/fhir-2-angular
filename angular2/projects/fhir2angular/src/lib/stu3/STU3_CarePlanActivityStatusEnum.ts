import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_CarePlanActivityStatusEnum{
    CANCELLED = 'cancelled',
    COMPLETED = 'completed',
    IN_PROGRESS = 'in-progress',
    NOT_STARTED = 'not-started',
    ON_HOLD = 'on-hold',
    SCHEDULED = 'scheduled',
    UNKNOWN = 'unknown',
}
