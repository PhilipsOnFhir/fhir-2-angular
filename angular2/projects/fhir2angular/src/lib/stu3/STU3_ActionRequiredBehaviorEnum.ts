import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_ActionRequiredBehaviorEnum{
    COULD = 'could',
    MUST = 'must',
    MUST_UNLESS_DOCUMENTED = 'must-unless-documented',
}
