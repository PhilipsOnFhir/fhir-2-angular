import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_EventStatusEnum{
    ABORTED = 'aborted',
    COMPLETED = 'completed',
    ENTERED_IN_ERROR = 'entered-in-error',
    IN_PROGRESS = 'in-progress',
    PREPARATION = 'preparation',
    SUSPENDED = 'suspended',
    UNKNOWN = 'unknown',
}
