import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_MedicationDispenseStatusEnum{
    COMPLETED = 'completed',
    ENTERED_IN_ERROR = 'entered-in-error',
    IN_PROGRESS = 'in-progress',
    ON_HOLD = 'on-hold',
    PREPARATION = 'preparation',
    STOPPED = 'stopped',
}
