import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_UDIEntryTypeEnum{
    BARCODE = 'barcode',
    CARD = 'card',
    MANUAL = 'manual',
    RFID = 'rfid',
    SELF_REPORTED = 'self-reported',
    UNKNOWN = 'unknown',
}
