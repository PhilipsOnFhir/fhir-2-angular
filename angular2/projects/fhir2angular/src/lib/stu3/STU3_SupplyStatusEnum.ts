import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_SupplyStatusEnum{
    CANCELLED = 'cancelled',
    DISPENSED = 'dispensed',
    FAILED = 'failed',
    RECEIVED = 'received',
    REQUESTED = 'requested',
}
