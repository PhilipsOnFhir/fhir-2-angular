import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_ParticipationStatusEnum{
    ACCEPTED = 'accepted',
    DECLINED = 'declined',
    NEEDS_ACTION = 'needs-action',
    TENTATIVE = 'tentative',
}
