import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_StructureMapSourceListModeEnum{
    FIRST = 'first',
    LAST = 'last',
    NOT_FIRST = 'not_first',
    NOT_LAST = 'not_last',
    ONLY_ONE = 'only_one',
}
