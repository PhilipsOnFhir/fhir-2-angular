import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_ReferenceHandlingPolicyEnum{
    ENFORCED = 'enforced',
    LITERAL = 'literal',
    LOCAL = 'local',
    LOGICAL = 'logical',
    RESOLVES = 'resolves',
}
