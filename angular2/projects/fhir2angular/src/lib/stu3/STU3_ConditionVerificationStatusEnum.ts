import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_ConditionVerificationStatusEnum{
    CONFIRMED = 'confirmed',
    DIFFERENTIAL = 'differential',
    ENTERED_IN_ERROR = 'entered-in-error',
    PROVISIONAL = 'provisional',
    REFUTED = 'refuted',
    UNKNOWN = 'unknown',
}
