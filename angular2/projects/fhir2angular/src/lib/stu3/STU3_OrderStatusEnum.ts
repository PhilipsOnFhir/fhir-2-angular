import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_OrderStatusEnum{
    ABORTED = 'aborted',
    ACCEPTED = 'accepted',
    CANCELLED = 'cancelled',
    COMPLETED = 'completed',
    ERROR = 'error',
    PENDING = 'pending',
    REJECTED = 'rejected',
    REPLACED = 'replaced',
    REVIEW = 'review',
}
