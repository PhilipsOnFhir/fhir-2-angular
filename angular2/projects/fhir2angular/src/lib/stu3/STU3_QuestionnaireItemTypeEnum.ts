import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_QuestionnaireItemTypeEnum{
    ATTACHMENT = 'attachment',
    BOOLEAN = 'boolean',
    CHOICE = 'choice',
    DATE = 'date',
    DATETIME = 'dateTime',
    DECIMAL = 'decimal',
    DISPLAY = 'display',
    GROUP = 'group',
    INTEGER = 'integer',
    OPEN_CHOICE = 'open-choice',
    QUANTITY = 'quantity',
    REFERENCE = 'reference',
    STRING = 'string',
    TEXT = 'text',
    TIME = 'time',
    URL = 'url',
}
