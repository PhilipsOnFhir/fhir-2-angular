import { STU3_DomainResource } from './STU3_DomainResource';

export enum STU3_TestReportStatusEnum{
    COMPLETED = 'completed',
    ENTERED_IN_ERROR = 'entered-in-error',
    IN_PROGRESS = 'in-progress',
    STOPPED = 'stopped',
    WAITING = 'waiting',
}
